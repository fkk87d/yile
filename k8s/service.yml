apiVersion: v1
kind: Service
metadata:
  name: ikm74r
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ikm74r"
    port: 8080
  selector:
    app: ikm74r
