apiVersion: v1
kind: Service
metadata:
  name: kfe53f
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "kfe53f"
    port: 8080
  selector:
    app: kfe53f
